{
  "puppetdb": {
    "server_urls": <%= [@server_urls].flatten %>,
    "cacert": "<%= @config_rootdir %>/puppet/ssl/certs/ca.pem",
    "cert": "<%= @config_rootdir %>/puppet/ssl/certs/<%= @certificate_filename %>",
    "key": "<%= @config_rootdir %>/puppet/ssl/private_keys/<%= @private_key_filename %>"
  }
}
