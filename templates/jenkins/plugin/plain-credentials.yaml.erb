---
credentials:
  system:
    domainCredentials:
    - credentials:
      <%- [@configuration].flatten.each do |credential| -%>
      - string:
          id: '<%= credential['id'] %>'
          secret: '<%= credential['secret'] %>'
          scope: GLOBAL
      <%- end -%>
