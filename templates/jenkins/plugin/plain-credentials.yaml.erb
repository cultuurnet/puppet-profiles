---
credentials:
  system:
    domainCredentials:
    - credentials:
      <%- @configuration['credentials'].each do |credential| -%>
      - string:
          id: '<%= credential['id'] %>'
          secret: '<%= credential['secret'] %>'
          scope: GLOBAL
      <%- end -%>
