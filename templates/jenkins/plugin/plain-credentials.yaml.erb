---
credentials:
  system:
    domainCredentials:
    - credentials:
      <%- [@configuration['credentials']].flatten.each do |credential| -%>
      - string:
          id: '<%= credential['id'] %>'
          secret: '<%= credential['secret'] %>'
          scope: GLOBAL
      <%- end -%>
